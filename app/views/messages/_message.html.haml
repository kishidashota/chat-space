.date
  .date--name
    = message.user.name
  .date--date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
.list--message
  - if message.body.present?
    %p.message--content
      = message.body 
  = image_tag message.image.url, class: 'message--content' if message.image.present?
